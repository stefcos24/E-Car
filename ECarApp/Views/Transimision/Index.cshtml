@model IEnumerable<ECar.Models.Transimision>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6 p-0">
            <h2 style="text-transform:uppercase; font-size:30px;">Transimision Types</h2>
        </div>
        <div class="col-6 text-right" style="text-align:right;">
            <a asp-controller="Transimision" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> 
                 Create New Transimision Type
            </a>
        </div>
    </div>
</div>


@if (Model.Count() > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th scope="col">ID</th>
                <th scope="col">Transimision Type</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td>@obj.Id</td>
                    <td>@obj.TransimisionType</td>
                    <td class="text-center">
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Transimision" asp-route-id="@obj.Id" asp-action="Edit" class="btn btn-primary"><i class="bi bi-pencil"></i> Update</a>
                        </div>
                    </td>
                    <td class="text-center">
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Transimision" asp-route-id="@obj.Id" asp-action="Delete" class="btn btn-danger"><i class="bi bi-trash"></i> Delete</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

